import type {SecurityPlusContentPack,ContentQuestion} from './types.js';
import {securityPlusContentPack} from './security-plus-v3.2.js';
const allowedTypes=new Set(['scenario','quick-check','compare','workplace-example','acronym','challenge']);
export function validateContentPack(pack:SecurityPlusContentPack){const errors:string[]=[];if(pack.packId!=='security-plus-sy0-701')errors.push('Unexpected content-pack identifier.');if(pack.compatibilityVersion!=='3.2')errors.push('This content pack is not compatible with the current application.');if(pack.curriculumVersion!=='1.2')errors.push('Unsupported curriculum version.');const ids=new Set<string>();for(const q of pack.questions){if(!q.questionId||ids.has(q.questionId))errors.push(`Duplicate or missing question ID: ${q.questionId||'unknown'}`);ids.add(q.questionId);if(!q.conceptId||!q.unitId)errors.push(`Question ${q.questionId} has an invalid curriculum mapping.`);if(!allowedTypes.has(q.activityType))errors.push(`Question ${q.questionId} has an unsupported activity type.`);if(q.options.length<3||new Set(q.options).size!==q.options.length)errors.push(`Question ${q.questionId} has invalid answer options.`);if(!q.options.includes(q.correctAnswer))errors.push(`Question ${q.questionId} does not include its correct answer.`);for(const option of q.options)if(!q.explanations[option])errors.push(`Question ${q.questionId} lacks a specific explanation for ${option}.`);if(q.prompt.includes('selecting the most precise SY0-701 term'))errors.push(`Question ${q.questionId} uses the retired generic prompt.`)}if(errors.length)throw new Error(`Learning content could not load: ${errors.join(' ')}`);return pack}
export function loadContentPack(){return validateContentPack(securityPlusContentPack)}
export function eligibleQuestions(pack:SecurityPlusContentPack,mode:'learn'|'boss'|'exam'):ContentQuestion[]{return pack.questions.filter(q=>mode==='learn'?q.review.approvedForLearn:mode==='boss'?q.review.approvedForBoss:q.review.approvedForExam)}
