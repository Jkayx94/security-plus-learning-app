# AI Tutor Specification
Version 2 uses an offline grounded tutor interface rather than an external API. Responses are assembled only from the current approved record, objective branch, related concepts, memory cue, Microsoft/MSP context and exam distinction. The interface is ready for a later cloud provider behind the same context contract. No unsupported external facts are generated.
