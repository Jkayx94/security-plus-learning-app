# Deployment Notes
The included GitHub Actions workflow installs the dependency-free package, validates data, runs tests, type-checks, builds `dist`, uploads the Pages artifact and deploys it. The prior archive-extraction workflow is no longer required; this archive contains the complete repository tree directly.
